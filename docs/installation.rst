============
Installation
============

At the command line::

    $ easy_install django-rest-admin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-admin
    $ pip install django-rest-admin
